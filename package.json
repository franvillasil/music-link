{
  "name": "music-link",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc src/app.ts --target ES2020 --module ES2020 --lib DOM,ES2020 --outDir dist --skipLibCheck --strict --pretty false",
    "check": "npm run build",
    "serve": "npm run build && node --experimental-strip-types scripts/serve.ts"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
